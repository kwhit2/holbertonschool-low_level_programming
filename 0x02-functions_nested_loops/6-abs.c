#include "holberton.h"

/**
* int _abs - print_sign function that computes
* the absolute value of an integer.
* Return: r and -r
*
*/

int _abs(int r)
{
	if (r >= 0)
		return (r);

	else
		return (-r);
}
