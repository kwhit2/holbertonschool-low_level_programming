#include "holberton.h"
/**
* wildcmp -Compares two strings and returns 1 if the
* strings can be considered identical, otherwise return 0.
* @s1: string one, has no wildcards
* @s2: string two, can have a wildcard '*'
* Return: Returns 1 if the strings can be
* considered identical, otherwise return 0.
*/
int wildcmp(char *s1, char *s2)
{
    
}